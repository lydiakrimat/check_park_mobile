import 'dart:ui';

class AppColors {
  AppColors._();

  // --- Algérie Télécom brand ---
  static const primary      = Color(0xFF1B4D8E);
  static const primaryLight = Color(0xFF2D6FC4);
  static const primaryDark  = Color(0xFF0F2F5A);

  static const green        = Color(0xFF00A651);
  static const greenLight   = Color(0xFF00CC65);
  static const greenDark    = Color(0xFF007A3D);

  // --- UI neutrals ---
  static const background   = Color(0xFFEEF2F7);
  static const white        = Color(0xFFFFFFFF);
  static const text         = Color(0xFF0D1F3C);
  static const muted        = Color(0xFF5E7491);
  static const border       = Color(0xFFD0DCEA);

  // --- Status ---
  static const danger       = Color(0xFFE53935);
  static const warning      = Color(0xFFF59E0B);

  // Autorisé
  static const okBg         = Color(0xFFD1FAE5);
  static const okText       = Color(0xFF065F46);
  // Refusé
  static const noBg         = Color(0xFFFEE2E2);
  static const noText       = Color(0xFF991B1B);
  // Expiré
  static const expBg        = Color(0xFFF3F4F6);
  static const expText      = Color(0xFF6B7280);

  // --- Light tints for cards ---
  static const blueTint     = Color(0xFFEFF6FF);
  static const greenTint    = Color(0xFFF0FDF4);
  static const orangeTint   = Color(0xFFFFFBEB);
  static const redTint      = Color(0xFFFEF2F2);
}
